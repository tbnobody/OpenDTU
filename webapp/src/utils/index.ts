import { isLoggedIn, login, logout } from './authentication';
import { timestampToString } from './time';

export {
    timestampToString,
    login,
    logout,
    isLoggedIn,
};

export default {
    timestampToString,
    login,
    logout,
    isLoggedIn,
}